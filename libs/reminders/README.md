# reminders

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build reminders` to build the library.

## Running unit tests

Run `nx test reminders` to execute the unit tests via [Vitest](https://vitest.dev/).
