# chantiers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build chantiers` to build the library.

## Running unit tests

Run `nx test chantiers` to execute the unit tests via [Vitest](https://vitest.dev/).
